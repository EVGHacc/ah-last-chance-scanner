name: AH dispatch smoke target

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo 'workflow dispatch permission works'
